# Dados sensiveis (nunca commitados)
.env
.cctl-instance

# Estado/override runtime do rollout Blue/Green (gerados durante
# `cctl rollout`, nunca versionados — ver lib/rollout.sh)
.cctl-rollout
docker-compose.rollout.yaml

# Arquivos de sessão local (não vão para o repositório público)
WORK_LOG.md
CLAUDE.md

# Logs
logs/
*.log

# Arquivos temporarios
*.tmp
*.bak
*.swp
*~

# OS
.DS_Store
Thumbs.db

# Frameworks de teste vendorizados (buscados via tests/bootstrap.sh, nao versionados)
tests/vendor/
