# cctl.conf — Defaults globais do containers-control

# Diretorio base das instancias no servidor
CCTL_INSTANCE_BASE_DIR="/var/docker"

# Registry (ex: ghcr.io/usuario)
CCTL_REGISTRY=""
CCTL_REGISTRY_USER=""

# Versao minima do Docker
CCTL_MIN_DOCKER_VERSION="24.0"
